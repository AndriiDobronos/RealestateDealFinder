# Server-only variables. Never put these into VITE_* variables.
DIM_RIA_API_KEY=replace_after_approval
# City and state IDs are now resolved automatically from state_ID.json.
# DIM_RIA_STATE_ID and DIM_RIA_CITY_ID are no longer required.
