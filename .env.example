# Server-only variables. Never put these into VITE_* variables.
DIM_RIA_API_KEY=replace_after_approval
# Get these IDs through the DIM.RIA cities endpoint after the key is approved.
DIM_RIA_STATE_ID=replace_with_state_id
DIM_RIA_CITY_ID=replace_with_city_id
